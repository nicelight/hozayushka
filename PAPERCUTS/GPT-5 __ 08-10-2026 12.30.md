# Papercuts

- `./gradlew assembleDebug` emitted the known SDK tooling warning that the installed processor understands XML up to version 3 while an SDK XML version 4 was encountered; the build still completed successfully.
- The first `uiautomator dump` on the live main shell returned `ERROR: could not get idle state` and produced no remote XML; the screenshot and activity/window dumps were available, so the hierarchy capture was retried separately.
