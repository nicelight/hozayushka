# Session papercuts

- The first W26 claim probe used a Kotlin assertion lambda form unsupported by
  the project's JUnit overloads, so it failed during test compilation rather
  than producing a valid RED. The probe was corrected to the existing
  message-plus-Boolean assertion form before rerun; no production code was
  changed by the failed attempt.
