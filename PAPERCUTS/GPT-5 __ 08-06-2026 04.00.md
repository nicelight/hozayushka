Команда `python` отсутствует в окружении; bounded schema/DAG validation продолжена через `python3`.
Первый regex для подсчёта module rows boundary-map не учёл Markdown-ссылки; повторная проверка использовала границы таблицы.
Feature lifecycle `status: blocked` не прошёл project lint; для этого формата blocker должен быть выражен через `spec_design_status: blocked`.
