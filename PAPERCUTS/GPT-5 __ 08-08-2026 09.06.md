# Workflow papercuts

- Initial read attempted the global `/home/serg/.codex/skills/r5/...` path, but this project maps `r5` to `.agents/skills`; continued with the correct project-local skill paths.
- An adversarial ownership probe initially matched Timer's own `stateStore.save(TimerRecord(...))` while checking for forbidden Settings writes; narrowed the check to Settings-qualified paths and continued.
