---
description: Session papercuts for W31 FT-001 planning.
status: active
---
# Session papercuts

- The installed Node `ajv` package rejected the task schema's JSON Schema
  2020-12 meta-schema because the default Ajv instance has no 2020-12 loader.
  The check stopped before validating the task card; use a compatible local
  validation path or a structural fallback.
- A shell inspection command split filenames containing spaces because it used
  unquoted `ls` output in command substitution. Use `find`/quoted paths for
  papercut filenames.
- The Python `jsonschema` package is not installed in the workspace, so the
  fallback draft-2020-12 validator was unavailable; use the installed Ajv
  2020 entry point or structural JSON/index checks.
