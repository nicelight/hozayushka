`ajv` is installed as the system package but only supports an older JSON Schema draft; compiling the repository's draft-2020-12 task schema failed before validation. The review continued with JSON parsing, index/dependency integrity checks, and direct schema-contract inspection without running doctor.
