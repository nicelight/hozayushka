The first verifier shell scan used a single-quoted regex containing an embedded single quote and failed before executing. The scan was rerun with fixed-string patterns and explicit file paths.
The first XML summary used `rg -h`, which is the help flag rather than no-heading in this ripgrep version; the totals command was corrected to `--no-heading --no-filename`.
The first index preflight display used the wrong split index for the wave label (`Wundefined`); the validation was corrected to use the sixth segment and rerun.
The first large verification-file patch used stale exact context (`FT-004-AC` versus the file's `FT-004 AC`) and was rejected without changing files; the verifier-owned file was then replaced with the final content using exact path ownership.
