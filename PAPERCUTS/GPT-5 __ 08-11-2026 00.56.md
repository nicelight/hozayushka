- `mb-doctor` classifies valid task-card SDD locators with `#fragment` as missing because `classifySddSpecLinks` passes the full locator, including the fragment, to `isFile`; `mb-lint` resolves the Markdown links successfully, so the resulting SDD/handoff warnings are misleading until the validator strips fragments before the filesystem check.
