# Papercuts

- Direct standalone schema probe could not import `ajv`; the project does not expose it through local Node module resolution. Project-native `node scripts/mb-lint.mjs` remains the schema/lint validation route.
