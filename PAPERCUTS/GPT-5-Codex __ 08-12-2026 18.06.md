## Workflow papercut

- The project-local ajv package could not be resolved by the direct Node
  schema-check command (ERR_MODULE_NOT_FOUND); node scripts/mb-lint.mjs
  remained available and passed.
- The first custom dependency check attempted to parse YAML
  spec-backbone.md as JSON; the corrected check reads its Planning Revision
  marker as text and passed.
- A combined read-only git-status command had an unmatched shell quote; the
  command was corrected and rerun without mutation.
