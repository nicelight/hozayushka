# Session papercuts

- The installed Node `ajv` package failed to compile the repository's draft 2020-12 task schema because its default validator does not register the `https://json-schema.org/draft/2020-12/schema` meta-schema. The probe was retried with a compatible validation approach.
- An inline Node validation probe initially failed because the orchestration wrapper parsed a nested JavaScript template literal; the corrected probe then exposed only a false-negative requirement-string matcher, not a reviewed-artifact defect.
