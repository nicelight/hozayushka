# Papercuts

- The Python `jsonschema` package was unavailable; the first Node `ajv` probe also failed because the installed Ajv entry point did not preload the draft-2020-12 meta-schema. The equivalent Node `ajv/dist/2020` validator was available and completed successfully.
