# Papercuts

- Secret-scan shell command failed before execution because nested quoting around a backtick-containing regex was malformed; no project artifact was modified.
- First corrected scan was intentionally over-broad and matched the phrase `real credential` in safety prose; narrowed scan is required, with no artifact modification.
- Second scan was over-broad around Kotlin `apiKey` type/field names and returned a false positive; no artifact modification.
- Third scan used `rg -E` (encoding) instead of `rg -e` (regexp), so its owner-bypass branch was not authoritative; corrected fail-closed scan follows.
- Git mtime boundary probe emitted expected `stat` errors for pre-existing deleted Yandex paths; surviving-file results remained usable and no project artifact was modified.
