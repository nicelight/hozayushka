# Papercut

- A targeted Gradle `testDebugUnitTest --tests ...` run rewrote the test-result directory to only the selected suites, temporarily removing the previously available `ForecastSessionTest.xml`. The full host suite must be rerun before using XML paths as durable evidence.
