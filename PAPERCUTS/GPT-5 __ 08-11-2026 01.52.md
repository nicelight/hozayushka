# Papercuts

- Generic `strings -el` did not expose expected Android `resources.arsc` values and caused an otherwise valid chained package-inspection command to stop. Use the installed SDK `aapt2 dump resources` for packaged Android resource verification.
