# Session papercuts

- The first attempt to read the project planning skills used the global
  `.codex/skills` path, but this repository's DevRails skills are installed
  under `.agents/skills`; the read was retried with the project-local path.
- A read-only summary command briefly passed Markdown `spec-backbone.md` to
  `jq`, producing a parse error; no project file was changed by that command.
- `adb shell cmd window is-keyguard-locked` is unsupported on the TECNO LI6
  build (`Unknown command`); unlock evidence was obtained from the focused
  MainActivity, screen-on state and absence of the lockscreen window instead.
