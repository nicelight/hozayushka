---
description: Session papercuts for GPT-5 on 2026-08-08 at 09.01.
status: active
---
# Session papercuts

- The first FT-009 static boundary command treated Timer & Alert's legitimate
  `requestAlertAudio` call through the registered Android Runtime Adapter as a
  direct platform bypass and exited early. The check was narrowed to reject
  direct provider/platform imports and policy calls only in Settings/Main
  Display, while preserving Timer's accepted contract call.
- A final test-report counter was run after `clean assembleDebug`, which had
  removed the unit-test XML report, and therefore printed `tests=0` without
  failing the shell command. The unit gate was rerun after the clean build
  before recording the final count.
