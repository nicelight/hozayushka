# Papercuts

- The first read-only task-graph probe over-escaped `\d` in a JavaScript regex literal and produced false ID-pattern errors for every indexed task. Corrected the probe and reran it; no project artifact was changed.
- The system Ajv entrypoint rejected the task schema's Draft 2020-12 `$schema` URI because only the legacy entrypoint is installed. Retried with the metadata removed in-memory; no schema file was changed.
- A simple custom Markdown slugger treated the slash in `Claim-Linked RED / GREEN For T2/T3` incorrectly and falsely flagged the valid double-hyphen fragment. The independent constitution-heading check still confirms the target card's `#product-non-negotiables` locator is genuinely unresolved; the canonical heading is `#v-product-non-negotiables`.
- A follow-up scoped locator probe initially treated task evidence text after `#fragment:` as part of the path because invariant entries carry explanations; rerouted it to split the locator before the explanatory suffix. No project artifact was changed.
