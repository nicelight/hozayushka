## Papercut

- The first read-only schema/DAG validation attempt used a Node ESM import for
  `ajv`; this workspace exposes Ajv through the system CommonJS resolution
  path, so the ESM import failed with `ERR_MODULE_NOT_FOUND`. Re-run the same
  validation with the available CommonJS loader.
