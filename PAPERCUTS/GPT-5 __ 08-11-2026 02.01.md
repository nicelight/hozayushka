# Papercuts

- A combined read-only shell probe failed before execution because nested quote
  escaping in one PCRE pattern was malformed. The probe was split into simpler
  marker, credential-shape and resource commands; no source, task state or
  generated evidence was affected by the failed invocation.
