# Papercuts

- A read-only multi-file `sed` inspection used invalid range syntax, producing command errors; reran the inspection with separate valid ranges.
- A local `github-slugger` module was unavailable during a read-only anchor sanity check; the canonical locator was verified from the heading and existing sibling task links instead.
