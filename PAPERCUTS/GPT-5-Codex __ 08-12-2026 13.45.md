## Papercut

- The optional inline AJV schema probe could not start because the repository
  environment does not expose the `ajv` package to Node module resolution.
  JSON parsing, project `mb-lint`, identity/index/dependency, and task-link
  checks remained available and passed.
- The first inline Markdown-anchor probe over-escaped its Unicode slug regex
  and reported a false missing anchor; the corrected probe was rerun without
  changing project artifacts.
- A combined final `jq` summary had one missing closing parenthesis and was
  discarded; the independent JSON/schema/link checks had already passed.
