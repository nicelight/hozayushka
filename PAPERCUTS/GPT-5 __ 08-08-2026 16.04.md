# Papercuts

- A broad discovery command included the plausible but absent `.memory-bank/specs` directory, so `rg` returned a non-zero missing-path diagnostic despite completing the useful reads. Prefer resolving registered paths from `spec-index.md` before passing optional directories to `rg`.
- A host `date +%s%3N` arithmetic probe printed impossible elapsed values despite a 2.6-second tool wall time. Its timing labels were discarded; the decisive city-double-tap check was rerun with explicit 250 ms and additional 750 ms waits in separate commands.
