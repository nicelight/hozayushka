# Papercut

- The installed Node `ajv` package rejected the repository task schema before
  validating task cards because it does not register the declared JSON Schema
  draft 2020-12 meta-schema. No reviewed artifact changed; structural checks
  continue with JSON parsing and bounded schema-shape probes.
