# Workflow papercuts

- Read-only acceptance-trace check used `rg -c` with an `awk` aggregation that reported `0` despite 14 matching checklist labels; direct `rg -n` confirmed all 14 labels. No evidence or production state was affected.
