# Papercut

- The first combined `apply_patch` for Attempt 2 reports did not apply because
  the existing verification protocol context differed from the expected patch
  lines. No files were changed by that failed patch; the workaround is to
  replace the task-owned protocol files explicitly and add new Attempt 2
  artifacts.
