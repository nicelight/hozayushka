Minor workflow friction: combined reads of the governing documents exceeded the tool output budget and were truncated. Re-read the relevant documents in smaller bounded chunks before completing the review.

Minor workflow friction: an inline preservation probe used an incorrect expected
`forbidden_scope` count and failed before the actual artifact guard; the task
card was re-read and the probe was corrected to the observed eight entries.

Minor workflow friction: one read-only command used a mistyped workdir
(`/home/serG` instead of `/home/serg`) and failed to start; no project artifact
was changed.

Minor workflow friction: a read-only `jq` filter for excluding FT-000 entries
used ambiguous operator precedence and emitted a type error; the data was not
changed and the invariant was rerun with explicit parentheses.

Minor workflow friction: a read-only source probe was rejected before launch
because its shell text contained `rm -f`; the probe was rewritten without a
destructive cleanup command and no project artifact was changed.
