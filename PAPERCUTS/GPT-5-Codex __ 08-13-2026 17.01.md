# Session papercuts

- W30 task closure stores `accepted_evidence` directly on the closure object,
  not under `gate_summary`; the initial jq path returned `0` and a manual
  count briefly used `18`. Re-read the authoritative JSON and corrected the
  sync report/changelog to the actual count of `19`.
