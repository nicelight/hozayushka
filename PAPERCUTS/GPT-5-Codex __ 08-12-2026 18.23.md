## Workflow papercut

- A first `jq` inspection treated `.memory-bank/tasks/index.json` as an array;
  the authoritative file is an object with a `tasks` array. The check was
  corrected without modifying task state.
