## Workflow papercut

- Focused W21 geometry probe initially failed during test compilation because
  the new internal `MainDisplayGeometry` helper was not imported by
  `DisplayProjectionTest.kt`. The implementation was otherwise not executed;
  the import was added inside the task's existing test boundary before rerun.
