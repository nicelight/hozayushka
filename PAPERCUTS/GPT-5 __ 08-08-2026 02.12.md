# Papercuts

- A safe `mktemp -d` probe command was rejected because its EXIT trap used
  `rm -rf` to clean only that resolved temporary directory; the command guard
  classified the cleanup as a forbidden broad `rm -f` style operation.
