The broad forbidden-path `git status` check reported pre-existing W16/W17
adapter and scheduler changes in the intentionally dirty workspace. The
task-local execution paths were checked separately; no W20 command edited those
historical files. Future checks must distinguish the preflight snapshot from
the current task-owned edit set.

The final scan's first summary label accidentally said
`no_live_network_or_device=used` despite using no such capability; the command
was rerun with the correct `not_used` label before evidence was recorded.
