# Papercuts

- Inline canonical-anchor check initially rejected the established
  `tier-policy.md#claim-linked-red--green-for-t2t3` task locator because a
  generic GitHub-slug implementation collapsed the slash differently; the
  project’s existing task-card convention and `mb-lint`/strict-doctor accept
  the established locator.
