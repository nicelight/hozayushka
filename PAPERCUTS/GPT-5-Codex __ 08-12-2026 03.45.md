# Session papercuts

- A targeted `apply_patch` used stale feature-index context and was rejected
  without changing files; the edit was retried in smaller exact-context
  patches.
- The first sync-local relative-link probe resolved `.memory-bank/tasks/plans/`
  paths with two parent traversals instead of three; the broken targets were
  corrected to workspace-root-relative paths and the check passed.
