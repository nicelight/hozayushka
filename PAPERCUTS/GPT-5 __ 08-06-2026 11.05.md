- The installed `ajv` validator rejected the task schema before validating the task cards because it did not have the declared JSON Schema draft 2020-12 meta-schema. The cards remained unchanged; validation continued with JSON parsing, required-shape inspection, and semantic checks.
- A supplemental report-link probe initially resolved relative links from the repository root instead of the report directory and falsely listed every relative link as broken. The project-native `mb-lint` still passed; the probe was rerouted with the correct base directory.
