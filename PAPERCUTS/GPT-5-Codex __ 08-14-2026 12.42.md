## Session papercut

- A bounded `find /tmp` discovery emitted multiple `Permission denied` lines
  from systemd-private directories before returning the relevant files. The
  task continued using the explicitly named evidence paths and project-local
  search results.
- The system `ajv` package is an older release without draft 2020-12 support;
  direct compilation stopped on the schema `$schema` URI before validating the
  task. Validation continued with the schema rules and repository-native
  checks available in the workspace.
- The shell has no `python` executable; the validation probe must use the
  installed `python3` name when a Python helper is needed.
- `python3` is present but has no `jsonschema` module; the draft-2020 schema
  check therefore uses an explicit schema-contract validator below instead of
  adding/installing tooling during planning.
