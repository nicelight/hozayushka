---
description: Minor workflow friction recorded during the TASK-034 W31 review session.
status: active
---
# Papercut

- TECNO firmware does not support `adb shell cmd window is-keyguard-locked`;
  the unsupported command returned `Unknown command`, so unlock state was
  established from focused MainActivity, `isKeyguardShowing=false`, screen ON,
  and the fresh physical screenshot instead.
- The host has a pre-existing Docker Desktop `qemu-system-x86_64` process;
  process inspection therefore cannot be used as evidence that this review
  started an emulator/AVD/QEMU. Review commands used only the authorized
  `adb -s 1156725456009666` physical target and no emulator launch command.
