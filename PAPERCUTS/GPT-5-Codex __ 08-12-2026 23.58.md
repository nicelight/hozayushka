AJV schema validation could not compile the repository's draft-2020-12 task schema because the system-installed AJV lacks the draft-2020-12 meta-schema. The repository-native `node scripts/mb-lint.mjs` and an independent JSON/schema/index/dependency check were used instead; no dependency installation or network action was attempted.
