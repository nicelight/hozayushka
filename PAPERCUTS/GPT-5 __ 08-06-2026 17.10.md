Read-only JSON Schema probe initially used an ESM `ajv` import that Node could
not resolve from the system module path; the project artifacts were unchanged.
The local jq 1.8.1 build does not support the older `--argfile` option; the
structural probe was rerouted to `--slurpfile` without changing project files.
The final parallel read-only check completed its shell commands, but the
wrapper attempted to read a loop variable outside its scope; results were
already emitted and no project artifact was changed.
