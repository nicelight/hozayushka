## Workflow papercut

- A bounded Python source-shape probe failed because an integer count was concatenated to a string without conversion. The probe was read-only and was rerun with corrected formatting.
