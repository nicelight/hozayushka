# Session papercuts

- A broad combined read of the canonical contract/task surface exceeded the tool output budget and was truncated; subsequent reads were narrowed to individual authoritative files and headings.
- The first local task-validation script used a non-existent regex capture group for `wave`; the validation was corrected and rerun without changing project artifacts.
- A heading-slug validator initially rejected the required stable feature AC locator form (`#FT-002-AC-004`); the skill contract uses the exact AC ID rather than the renderer-generated longer heading slug, so AC IDs were validated against their headings separately.
- The same local heading check did not model the installed workflow's preserved double-hyphen anchor for `Claim-Linked RED / GREEN`; the task keeps the canonical skill anchor and the check was narrowed to verify file/contract locators without rewriting them.
- The first schema probe could not compile the draft 2020-12 task schema because the installed Ajv wrapper lacked that meta-schema; the check was rerouted to a compatible validator/structural pass without changing project artifacts.
- The first rerun's plan assertion expected prose instead of the table-shaped dependency row; the check was narrowed to the canonical queue row and passed without changing project artifacts.
