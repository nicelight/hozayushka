# Papercuts

- The authorized `Tecno_Pova_6_API_35` AVD repeatedly became `offline` or terminated its QEMU process during fresh public probing; restarting it and retrying with SwiftShader/headless graphics still ended in a QEMU CPU-thread hang. The existing successful generic-emulator evidence remains usable as task evidence, while this session's partial runtime observation is not treated as a semantic finding.
- The host image utility `montage` is unavailable, so the existing runtime screenshots were inspected individually or through their recorded evidence instead of a generated contact sheet.
