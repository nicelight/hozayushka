---
description: Session papercuts.
status: active
---
# Papercuts

- A first atomic `apply_patch` failed because a multiline router context did not match the file's actual line wrapping; a second minimal patch succeeded.
- An initial sync-local path validator incorrectly treated prose `verification_targets` entries as filesystem paths and reported false missing paths; the validator was narrowed to path-bearing task fields.
