# Session papercuts

- A combined `apply_patch` for final evidence/protocol updates failed because
  one context hunk included an extra unmatched line. The patch made no partial
  changes; the same updates were applied successfully in smaller patches.
