- A graph-diagnostic `jq` command applied an object filter to multiple task JSON files (and the index), producing `Cannot index ...` shape errors before the intended dependency check. The reviewed files were not changed; the check was rerouted to a normalized input.
