# Session papercuts

- A parallel read batch used an incomplete `sed -n` range expression and returned `missing command`; the files were reread with the corrected command.
- The ad-hoc Ajv validator could not compile the repository's Draft 2020-12 task schema because the installed Ajv instance lacked its meta-schema; project-native `mb-lint` remained available and passed.
- A helper assertion counted repeated task-ID references inside the new card instead of checking task-file/index-entry uniqueness; the validation was rerun with file/entry identity checks.
