# Papercut

- The first sync-local validation used an overly exact multiline string
  assertion for an FT-003 route/risk check; the assertion failed despite the
  durable links being present and was corrected to use stable markers.
- A second validation assertion repeated the same over-specific markdown-link
  assumption for the preserved W19 route; it was narrowed to stable lifecycle
  and evidence markers.
- The report check then assumed a single-line residual-risk phrase despite
  Markdown wrapping; it was narrowed to independent `APPROVE` and
  `DEFERRED`/`runtime PASS` markers.
