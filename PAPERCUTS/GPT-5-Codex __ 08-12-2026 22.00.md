---
description: Minor workflow friction recorded for the current Codex session.
status: active
---
# Session papercut

The first task-index inspection treated `.memory-bank/tasks/index.json` as a
top-level array; the repository stores records under its `tasks` property, so
the initial jq summary failed. A later combined patch also referenced this
new papercut before it existed and was rejected atomically. The checks were
corrected without changing task or planning artifacts.

The first local Ajv schema audit could not load the repository's declared
Draft-2020 meta-schema because the installed system Ajv package only ships an
older meta-schema. The audit was rerun with meta-schema validation disabled;
the task schema itself, records, links and DAG were still validated.

The first custom fragment check also rejected the accepted
`#claim-linked-red--green-for-t2t3` locator because it compared punctuation
literally to the rendered heading slug; the repository's `mb-lint` accepts the
alias. The check was adjusted to compare normalized heading tokens.

The optional Python `jsonschema` fallback was unavailable (`python` is not
installed and `python3` has no module); no dependency was installed or added.

One append attempt used a stale context line and was rejected without changes;
the note was then applied against the current file content.
