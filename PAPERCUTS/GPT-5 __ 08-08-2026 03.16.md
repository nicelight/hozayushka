`ProviderHourlyShapeProbe` Java launch initially used a mistyped Kotlin stdlib classpath path; compilation succeeded but runtime reported `NoClassDefFoundError: kotlin/jvm/functions/Function0`. Correcting the launch classpath is sufficient; no project or production state was changed.

The first corrected probe command was rejected by the shell wrapper because its chained `rm -rf` cleanup was classified as a destructive command. The probe was rerun without that chain; temporary paths were then handled explicitly.
