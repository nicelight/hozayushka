# Session papercuts

- After the clean build removed Gradle test-result XML, the first read-only
  report-count lookup targeted the now-absent `app/build/test-results/`
  directory; the host gates themselves had already passed and the reports
  will be regenerated by the next test run.
- The first read-only `jq` geometry check used the executor artifact's wrong
  nesting path for the GREEN values; it was corrected to
  `.green.acceptedModel...` and passed without changing any project artifact.
