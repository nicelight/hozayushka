# Papercuts

- A combined read-only inspection command attempted to read a non-existent historical RED-VERIFY report at `.tasks/TASK-002-T3-FT-000-W1/TASK-002-T3-FT-000-W1-S-RED-VERIFY-final-report-docs-01.md`; the directory contains no file with that name. Continued using the available Foundation task/evidence records.
- The installed `ajv` package cannot compile the repository's Draft 2020-12 task schema (`no schema with key or ref "https://json-schema.org/draft/2020-12/schema"`); continued with JSON parsing and direct schema-required-field/property checks, without running `/mb-doctor`.
- The environment also lacks Python's `jsonschema` module, so no alternate Draft 2020-12 validator was available; the task remained covered by direct schema-contract checks and `mb-lint`.
- An initial patch for the existing review request used a stale line context and was rejected without changing the file; reread the request and continued with an exact-context patch.
