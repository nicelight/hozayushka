- `node` check assumed `.memory-bank/tasks/index.json` was a top-level array; the project stores the entries under its `tasks` property. Re-ran the inspection with the correct shape.
- The preservation probe used an inverted `forbidden_scope` predicate and reported a false hard-boundary failure; replaced it with direct required-boundary assertions and reran.
