# Papercut

- A bounded-source probe initially scanned the whole `DisplayCapability.kt` and
  counted the separate Forecast ticker as a W13 Main Display start marker. The
  probe was narrowed to the Main Display-owned source range before recording
  evidence; no project behavior or task state changed.
- A retry of the bounded marker count used an over-escaped inline regex and
  failed before inspection; the same literal probe was rerun successfully.
