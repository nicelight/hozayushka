# Papercut

- `ajv` is installed but cannot compile the repository task schema because it does not support the schema's draft 2020-12 meta-schema; the review continued with read-only structural checks and direct artifact inspection.
- The optional Python `jsonschema` validator is not installed; no package installation or network access was attempted.
