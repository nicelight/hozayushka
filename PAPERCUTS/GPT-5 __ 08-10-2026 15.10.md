## Papercut

- A combined static/boundary/redaction probe exited silently because one
  boolean assertion was too broad and an embedded mixed-quote regex broke the
  shell command. The checks were decomposed into quote-safe read-only probes;
  no project behavior or task lifecycle state was changed.
- The task-local `red-verification.md` resume file was absent; the first
  replacement patch assumed it existed. The canonical red-verification
  template is present, so initialization was retried as an add-only report
  write; no production or lifecycle state was affected.
- Repository-wide `git diff --check` surfaced pre-existing trailing whitespace
  in an unrelated FT-002 planning report; the W15 correction used a targeted
  check and left the unrelated artifact untouched.
