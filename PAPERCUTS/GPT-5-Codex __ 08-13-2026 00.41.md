Workflow papercut: a broad preflight output was truncated by the tool response limit. Continued with narrower, line-scoped reads; no task scope or repository state was changed because of it.

One large multi-file `apply_patch` had a malformed hunk and was rejected atomically. Re-split the task-local evidence additions into smaller patches; no production/test change resulted from the failure.
