- При read-only сверке FT-002 task-plan approval путь к review-отчёту был сокращён вручную и дал harmless `No such file`. Для таких operational artifacts нужно сначала разрешать точное имя через `rg --files`, а не восстанавливать его по шаблону.
- Финальная read-only status-команда поместила Markdown backtick внутрь double-quoted shell pattern и завершилась shell quoting error. Для literal Markdown markers в shell search нужно использовать single-quoted pattern или искать более узкий текст без backtick.
