# Papercuts

- Secret-scan command initially failed before execution because nested shell
  quoting for the regex ended the command string early. No project, build or
  evidence state was changed; the scan was rerun with a safer literal pattern.
- APK badging probe initially used `aapt` from `PATH`, where it is unavailable;
  the command was rerun through the project Android SDK build-tools path. The
  pipeline also masked the first non-zero command status; subsequent probes use
  direct exit-code checks.
- The fresh Gradle verification emitted an SDK XML version compatibility warning
  before completing successfully. It did not change the build/test result; the
  warning is an environment/toolchain note, not a task failure.
