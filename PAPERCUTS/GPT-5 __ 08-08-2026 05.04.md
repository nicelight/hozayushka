Focused FT-004 compile probe failed because the new display routing helper omitted the existing `WeatherCardSlot` import. The fix was a local import-only correction before rerunning the focused tests.

The first full host-suite compile after adding the persistence assertion referenced the test-local `store` before declaring it. The fix was a local test setup declaration; no production behavior or scope changed.
