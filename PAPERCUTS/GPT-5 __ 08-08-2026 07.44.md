# Papercuts

- A final static `rg` scan included the evidence artifact that records the
  same `rg` command, so the command text self-matched `token` and returned
  exit 1. Re-run the scan over the source/test surface only; artifact text is
  not a product source-safety input.

- An append patch for the verifier probe used an overly specific wrapped-line
  context and failed without changing files; retrying with the exact tail
  context resolved the bookkeeping edit.
