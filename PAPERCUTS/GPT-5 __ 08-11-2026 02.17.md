- A task-owned credential-shape scanner initially scanned its own regex source
  and reported one false-positive candidate group. The corrected command keeps
  exact-marker coverage over the whole task surface while excluding only the
  scanner file from its generic credential-literal patterns.
