# Papercut

- Inline Node validation command failed before execution because of a mismatched quote in its `identity_history` output string. No project files were changed; the command was corrected and rerun.
