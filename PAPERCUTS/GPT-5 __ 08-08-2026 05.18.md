---
description: Workflow papercuts recorded during the TASK-006 Reviewer session.
status: active
---
# Session papercuts

- A parallel Gradle probe returned an opaque/short-lived session handle; polling
  the guessed process id failed with `Unknown process id`. Re-running the same
  focused probe and reading its generated XML report completed the check.
- The executor's documented redaction scan includes its own artifact path, so
  the literal regex in that artifact self-matches. A source-only scan was used
  for the independent evidence.
- A broad multi-file `apply_patch` failed on stale context in the already-dirty
  changelog/epic surfaces; splitting the mechanical sync into smaller patches
  completed the intended edits without touching task JSON or production code.
