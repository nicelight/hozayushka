# Papercuts

- `apply_patch` initially failed because the patch context assumed the W13
  evidence links started on a new line, while the current Markdown keeps the
  first link on the preceding sentence line. No files were changed by the
  failed patch.
- A broad corrective-scope check treated pre-existing W12/W13 Memory Bank
  edits in the dirty worktree as this turn's changes; the check was discarded
  and rerun with the requested target surfaces only.
