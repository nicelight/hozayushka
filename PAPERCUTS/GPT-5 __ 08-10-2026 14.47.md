## Papercut

- `jq '.[] | ...' .memory-bank/tasks/index.json` assumed the task index was an array; the project schema stores records under `.tasks`, so the command failed before the corrected lookup.
