# Session papercuts

- `adb shell uiautomator dump` failed with `could not get idle state` while the running app was continuously updating its display; the screenshot and activity/window evidence were still captured successfully.
