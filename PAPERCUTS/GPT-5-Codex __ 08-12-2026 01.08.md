# Papercut

- Standalone verifier probe compile initially assumed a Kotlin stdlib 1.8.20 cache path that is absent in this workspace; the command failed before execution. The project Gradle test/build path remains valid. Reused the actual cached Kotlin runtime path for the replacement probe.
- The first combined static scan used a broad `WeatherProvider` token check and an incorrect count extraction, so it exited before running the remaining gates; refined the scan to import-level ownership checks and line-count probes.
- The refined redaction scan treated an empty zero-match `rg -c` result as an empty integer; normalized the no-hit result to `0` before the final rerun.
- The first XML aggregate used the wrong `awk` quote-field offsets and reported `tests=0` despite green suite XML; corrected the offsets and reran the read-only aggregation.
