# Papercuts

- A read-only evidence search command used shell globs for task filenames that did not match the repository layout, producing `rg` no-such-file diagnostics. Replaced with indexed-file lookup and direct paths.
