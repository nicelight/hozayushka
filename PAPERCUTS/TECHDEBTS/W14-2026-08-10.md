---
scope: TASK-017-T3-FT-001-W14 / Weather Context projection snapshot reuse and invalidation
type: advisory-technical-debt
material_debt_confirmed: no
---
# W14 technical-debt advisory

## Проверенная область

Проверена ровно change surface `TASK-017-T3-FT-001-W14`: разделение
обязательных scalar clock reads и Weather Context projection rebuilds.

- Task card: `.memory-bank/tasks/TASK-017-T3-FT-001-W14.task.json`
  (`touched_files`, `verify`, `runtime_context`, `verification_targets`).
- Фактическая production surface:
  `app/src/main/kotlin/com/hozayushka/app/weather/WeatherCapability.kt`.
- Фактическая focused-test surface:
  `app/src/test/kotlin/com/hozayushka/app/WeatherContextTest.kt`.
- Normative basis: `.memory-bank/contracts/capability-interfaces.md`,
  `.memory-bank/contracts/weather-provider.md`,
  `.memory-bank/contracts/weather-card-presentation.md`,
  `.memory-bank/features/FT-001-main-clock-display.md`.
- W14 evidence: `.tasks/TASK-017-T3-FT-001-W14/verifier-owned-evidence.md`
  и `.protocols/TASK-017-T3-FT-001-W14/verification.md`.

Остальные dirty paths не приняты за W14 surface. W13 проверен только как
непереоткрываемый источник исходного `TD-W13-001`; W12, repo-wide findings,
Forecast, Timer & Alert, provider transport и target-device evidence в этот
вердикт не включались.

## Подтверждённые findings

Нет. Material debt, correctness risk или серьёзная optimization need в
проверенной W14 change surface не подтверждены.

Наблюдаемый механизм соответствует контракту: один private
`ProjectionSnapshot` владеется `WeatherCapability` и переиспользуется при
неизменном location/time/freshness/pressure input
(`WeatherCapability.kt:358-400`); rebuild сохраняет four-card, timezone,
day/night, pressure и stale/empty semantics (`:403-484`). Успешный accepted
refresh сохраняет record и перестраивает snapshot ровно через owner path,
тогда как provider failure возвращается до mutation (`:540-598`).

Focused tests подтверждают повторное чтение без `loadRecord()`
(`WeatherContextTest.kt:62-74`), invalidation после accepted refresh и
preservation после failed refresh (`:76-100`), а также location, pressure,
day/night и 24-hour freshness boundaries (`:102-134`). Полный host/static
receipt фиксирует clean build, 59/59 unit tests, 13/13 Weather Context tests и
`git diff --check` без failure
(`.tasks/TASK-017-T3-FT-001-W14/verifier-owned-evidence.md:8-20`).

## Impact, priority и remediation

Не применимо: подтверждённый finding отсутствует, поэтому remediation не
требуется. W13 residual decode/projection mechanism в этой surface устранён;
новое публичное API, boundary, dependency или owner не добавлены.

## Uncertainty

Размер фактического frame/GC выигрыша отдельно не профилировался, но это не
оставляет подтверждённого debt механизма: source-level повторный decode/build
устранён и bounded host evidence подтверждает reuse/invalidation. Samsung,
custom-ROM, 1280x720 и прочее target-device evidence остаётся явно deferred по
контракту W14 и не рассматривается как defect или finding.
