# W30 — advisory technical debt

- scope: `TASK-033-T3-FT-001-W30` / `FT-001` Main Display; W29 provenance disposition; the exact two-file Main Display diff; synced FT-001 docs; governing architecture, tier and testing contracts.
- verdict: `APPROVE` — W30 host-baseline acceptance is supported. This is not a release/runtime approval.
- execution boundary: read-only review; no test, build, runtime, emulator/device, adb, network, audio or `/mb-sync` action was run.

## Evidence checked

- W30 task card is `done`; executor `PASS_FOR_HANDOFF`, verifier `PASS`, and semantic `semantic-pass` are recorded in `.memory-bank/tasks/TASK-033-T3-FT-001-W30.task.json` and the W30 final reports.
- Fresh verifier-owned W30 evidence at `2460×1080` and `1280×720` proves the complete `HH:mm`, four ordered `YESTERDAY|TODAY|TOMORROW|DAY_AFTER` shells for `NO_DATA`/partial/populated projections, and the three preset radial/rim/glow receipts. The 1280×720 height delta is `1.5258789E-5 px`, inside the accepted `0.01 px` tolerance.
- W30 records the exact two-file diff as unchanged during the wave: `app/src/main/kotlin/com/hozayushka/app/display/DisplayCapability.kt` and `app/src/test/kotlin/com/hozayushka/app/DisplayProjectionTest.kt`; recorded hash: `2c18f3a6f99dcd617a24b6ebaac44894804b03de8eae55758f956fa99eb57fb2`.
- W29 is terminal `failed` for missing pre-write RED/executor provenance, explicitly not for an evidenced product semantic failure. W30 correctly did not promote W26/W28/W29 evidence.

## Confirmed debt

### HIGH — dirty worktree makes task attribution and RED provenance fragile

- Evidence: current read-only `git status` reports `225` status lines (`95` modified, `127` untracked, `3` deleted/other); the current two-file diff is `1231/124` lines for `DisplayCapability.kt` and `416/0` for `DisplayProjectionTest.kt`. W29 records that no durable pre-write RED or executor summary could be established because the broad worktree was dirty; W30 had to recover with a fresh `RED_NOT_APPLICABLE` baseline and independent probe.
- Impact: another bounded T3 Main Display change can again lose clean ownership, rollback, and claim-linked RED/GREEN attribution even when the product behavior is acceptable.
- Owner: operator/workflow owner; implementation owner for the next Main Display change.
- Smallest follow-up: isolate or commit the existing wave changes before the next two-file write, then record a clean status, exact baseline diff/hash, and pre-write task-owned RED in the same isolated surface.

### HIGH — real target density/fullscreen and visual readability remain unproven

- Evidence: W30 `target-device.md` is `DEFERRED`; the canonical release target is the Samsung GT-I9300I Android 11 custom ROM at 1280×720, while the TECNO Pova 6 API 35 run is explicitly a generic Google emulator. The prior TECNO review records HIGH findings for zero-height city/status rows and failed double-tap-anywhere cancellation, while its generic fullscreen/keep-screen-on PASS is not release-target evidence.
- Impact: W30 host geometry and the SVG/metric radial-rim-glow receipt establish the host model, not actual target font rasterization, system-panel behavior, contrast/readability, or touch reachability. The prior generic-runtime findings remain unclosed by W30.
- Owner: Main Display/runtime-readiness owner; target-device verification owner for the release route.
- Smallest follow-up: run the authorized runtime/readiness route on the canonical target (TECNO remains supplementary only), recheck city/gesture reachability, fullscreen/keep-screen-on and preset rim/glow readability, and preserve host-versus-target verdict separation.

### MEDIUM — provider refresh is outside the W30 proof surface

- Evidence: W30's weather regression is read-only projection coverage; its accepted residual risk says physical weather refresh/provider behavior remains deferred. The architecture and runtime-testing contracts assign selected-provider dispatch, refresh, freshness, cache/history and provider isolation to Weather Context, not Main Display.
- Impact: W30 proves stable shells and no fabricated values, but does not prove selected Open-Meteo/OpenWeather refresh, failure recovery, provider/location cache identity or target-visible refresh behavior.
- Owner: Weather Context / provider-migration follow-up owner (FT-002 route).
- Smallest follow-up: use the existing deterministic selected-provider fixture route to prove refresh, failure, freshness and cache isolation; add authorized target/runtime refresh evidence only where host checks cannot establish the requirement.

## No material W30 finding admitted

- Four-slot host shells pass at both required sizes with stable order, no layout shift and no fabricated values.
- Host radial shading, rim widths and exactly three static outward glow layers pass; no heavy/realtime effect or ownership bypass is evidenced.
- The W30 task uses `partial` as its fresh missing-data state rather than a separately named async lifecycle probe. The reviewed task contract accepts that matrix, and no defect is evidenced; this is recorded as uncertainty, not a debt finding.
- Target/runtime and provider-refresh items above are deferred follow-ups, not blockers under the governing runtime-verification and T3 policy. No W30 task/status/spec/checkpoint/terminal mutation is recommended.

