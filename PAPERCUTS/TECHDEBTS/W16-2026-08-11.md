---
description: Advisory technical-debt review for W16 FT-008 change surface.
status: advisory-only
scope: W16 / TASK-019-T3-FT-008-W16
date: 2026-08-11
material_debt_confirmed: false
---

# Technical-debt report — W16 / FT-008

## Checked scope

Проверена ровно W16 change surface, разрешённая из
`TASK-019-T3-FT-008-W16`:

- task card и task-owned execution/verification evidence в
  `.memory-bank/tasks/TASK-019-T3-FT-008-W16.task.json`,
  `.protocols/TASK-019-T3-FT-008-W16/` и
  `.tasks/TASK-019-T3-FT-008-W16/`;
- фактический production diff
  `app/src/main/kotlin/com/hozayushka/app/settings/SettingsCapability.kt` и
  `app/src/main/res/values/strings.xml`;
- фактический host-test diff
  `app/src/test/kotlin/com/hozayushka/app/SettingsLocationTest.kt`;
- только необходимые direct call sites в `WeatherCapability.kt` и
  `FoundationRuntime.kt`, а также прямой replacement contract
  `TASK-020-T3-FT-002-W17`, чтобы проверить границу временного key-access deny.

Широкий pre-existing dirty worktree вне этой поверхности не принимался за W16
debt и не изменялся данным обзором.

## Evidence checked

- Task card фиксирует три app-пути как advisory surface
  (`TASK-019-T3-FT-008-W16.task.json:17-20`), финальный Attempt 3 functional и
  semantic pass (`:74-93`) и принятый residual transition
  (`:114-116`).
- Текущий diff добавляет provider selection, отдельный OpenWeather key storage,
  contextual projection и сохранение legacy key только как удаляемого inert
  поля (`SettingsCapability.kt:48-64,196-220,248-320,359-371,381-452`). UI и
  attribution surface находятся в `SettingsCapability.kt:547-740`, строки — в
  `strings.xml:9-16`.
- Host regressions покрывают first-run/switch/reopen, generic-access deny,
  durable-secret absence, failure neutrality, content order и оба проверенных
  legacy refresh trigger (`SettingsLocationTest.kt:27-224,227-335,352-376`).
- Final verifier подтвердил ровно три app diff path, отсутствие forbidden
  adapter/transport/cache/forecast/dependency diff и чистые secret scans
  (`.protocols/TASK-019-T3-FT-008-W16/verification.md:62-95`), затем сопоставил
  все четыре task-owned AC с PASS (`:97-115`). Adversarial verification не
  допустил findings и завершился `semantic-pass`
  (`.protocols/TASK-019-T3-FT-008-W16/red-verification.md:20-71`).
- Исторические дефекты не скрыты: Attempt 1 raw evidence marker исправлен в
  Attempt 2, а Attempt 2 owner-key release/false attribution исправлены bounded
  Attempt 3 deny и neutral handling
  (`.protocols/TASK-019-T3-FT-008-W16/progress.md:11-21,84-91`;
  `.tasks/TASK-019-T3-FT-008-W16/red-green-evidence.md:62-121`).

## Confirmed findings

## Result

Материальный technical debt в проверенной W16 surface не подтверждён. Текущие
provider/key state, owner-local persistence, redaction boundary, Settings order
и regression probes не показывают material repeated-change cost, новый
неуправляемый coupling, ownership drift или подтверждённый reliability defect.

## Impact, priority and remediation

Не применимо: подтверждённых findings нет, поэтому priority и remediation не
назначаются.

## Risks and uncertainty

`SettingsCapability.withWeatherApiKey` намеренно возвращает `null`
(`SettingsCapability.kt:391-405`), из-за чего provider-unidentified legacy
refresh не получает owner key (`WeatherCapability.kt:618-643`). Это не принято
как finding: W16 evidence явно ограничивает механизм переходным safeguard, а
зависящий `TASK-020-T3-FT-002-W17` обязан атомарно заменить deny на
selected-OpenWeather-authorized access вместе с exactly-one selected-provider
dispatch (`TASK-020-T3-FT-002-W17.task.json:17-19,51-59,80-90,151-157`;
`.memory-bank/tasks/plans/IMPL-FT-002.md:43-49`). Проверенная evidence не
показывает дополнительного material maintenance/regression debt сверх этой
принятой последовательности.

Physical-device Settings/readability и live-provider integration остаются
`DEFERRED` без runtime `PASS`
(`.protocols/TASK-019-T3-FT-008-W16/verification.md:107-117`). Это ограничение
evidence и явный downstream route, а не подтверждённый debt-механизм W16.

## Workflow boundary

Отчёт advisory-only: он не меняет и не маршрутизирует code, tests, specs, tasks,
protocols, statuses, checkpoint, lifecycle, promotion или scheduler state.
