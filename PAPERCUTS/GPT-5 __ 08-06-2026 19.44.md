## Review session

- The first read-only validation probe used `python`, but this workspace exposes `python3` only; reroute validation commands to `/usr/bin/python3`.
- The fallback `python3` environment does not provide `jsonschema`; use the available Node/JQ validation path instead.
- A naive heading-slug checker collapsed the double hyphen in `#claim-linked-red--green-for-t2t3`; Markdown's slash-separated heading preserves that anchor form, so the probe result was not treated as a project defect.
- The system `ajv` package defaults to an older JSON-Schema draft and rejected the task schema's 2020-12 meta-schema; use `ajv/dist/2020` for the validation probe.
