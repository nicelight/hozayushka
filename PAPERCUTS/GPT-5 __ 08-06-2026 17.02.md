Один read-only shell probe завершился синтаксической ошибкой: несколько `sed` ranges были переданы в `for` без корректного quoting. Состояние проекта не изменено; чтение продолжено исправленным probe.
