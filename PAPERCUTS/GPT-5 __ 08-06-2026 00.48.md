# Papercut

- The environment does not provide the Python `jsonschema` package, so the
  first schema-validation attempt could not run. The FT-001 preflight was
  rerun with a bounded stdlib validator instead.
- `scripts/mb-lint.mjs` cannot pass the current workspace because the
  pre-existing `.memory-bank/tasks/plans/IMPL-FT-001.md` uses `status: blocked`,
  which is outside the plan-status schema.
- A shell heading command used `printf` with a format beginning in `--`, which
  the environment interpreted as an option; subsequent inspection used safe
  formatting.
