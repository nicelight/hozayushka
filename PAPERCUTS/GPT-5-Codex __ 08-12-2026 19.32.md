# Papercuts

- The first focused Gradle host probe emitted the repository/toolchain warning
  `SDK processing. This version only understands SDK XML versions up to 3 but
  an SDK XML file of version 4 was encountered.` The probe still completed
  successfully; no workaround or environment mutation was needed.
