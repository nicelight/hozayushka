# Papercut

- Independent boundary scan used a broad `WeatherCapability` pattern and falsely matched the registered `DisplayCapability -> WeatherCapability` public edge; rerun with exact private-store/provider-bypass patterns.
