- Initial multi-card reads exceeded the tool output limit and were truncated; switched to per-card/field-targeted reads before editing.
- The installed Ajv validator lacked the Draft 2020-12 meta-schema; validated the unchanged project schema through its supported structural subset instead of modifying the schema.
