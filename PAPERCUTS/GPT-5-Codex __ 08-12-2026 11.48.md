Inline Node structural probe failed on the first run because shell escaping
produced an unterminated regular expression; the probe was retried read-only
with simpler checks.
The first AJV invocation also failed because the generic AJV class did not
load the task schema's Draft 2020-12 meta-schema; validation was retried with
the Draft 2020-12 entry point.
