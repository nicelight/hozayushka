# Papercut

- Одноразовая read-only проверка FT-004 не запустилась из-за shell-quoting вложенного JavaScript validator; `mb-lint` при этом завершился успешно.
- Повторный validator некорректно схлопывал пробелы при построении GitHub-style anchors вокруг `/`; `mb-lint` снова проходил.
- Read-only schema probe сначала использовал неверный jq-ключ и ESM-import системного `ajv`, который не резолвится из workspace; повторная проверка переведена на доступный CommonJS resolution.
