## Papercut

- Local `ajv` default entry rejected the task schema's declared draft
  2020-12 meta-schema during read-only validation. The project schema is
  unchanged; the validation route must use Ajv2020 instead of adding or
  changing project dependencies.
