# Papercuts

- The system `ajv` package rejected the task schema before validating the task because it does not register JSON Schema draft 2020-12. The check was rerouted to structural `jq`/JSON validation; no planning or production artifact was changed.
