# Papercuts

- The installed Node `ajv` validator rejected the repository's JSON Schema draft 2020-12 `$schema` URI because its bundled meta-schema was unavailable; the failed invocation produced no project changes. Structural JSON, index, dependency, and DAG checks were rerouted through direct read-only checks.
