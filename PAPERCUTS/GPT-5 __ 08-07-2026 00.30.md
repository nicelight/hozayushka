## Planning session

- `mb-lint` initially reported broken links in the newly created `IMPL-FT-005.md` because paths from `.memory-bank/tasks/plans/` were written with a duplicated `.memory-bank/` segment; corrected to the repository-relative pattern used by existing implementation plans.
