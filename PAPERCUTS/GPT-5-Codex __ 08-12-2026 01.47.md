# Papercuts

- A read-only `jq` validation command initially applied object-field access to the mixed string/object `verify` array and exited with `Cannot index string with string "stage"`; rerun with an explicit type guard.
- A read-only `jq` status probe initially treated `.memory-bank/tasks/index.json` as a task array; the authoritative entries are nested under `.tasks`. Re-ran the lookup against the correct shape; no files changed.
