# Papercuts

- `/exe` preflight found the selected T3 task already `in_progress` but its five required protocol files were absent; an initial protocol glob also produced a missing-path error. The protocol was initialized from the framework templates before any production probe/write.
- A combined final bookkeeping patch had one stale-context hunk and was rejected without changing files; the follow-up patch is split by protocol file.
