- The first read-only doctor invocation targeted the non-existent `scripts/mb-doctor/index.mjs`; the project entrypoint is `scripts/mb-doctor.mjs`. No project artifact was changed by the failed invocation; the corrected command was rerun.
- The first direct schema probe could not resolve the system-installed `ajv` package from the project module path. The validation was rerouted to the available absolute system Ajv 2020 entrypoint; no schema or project dependency was changed.
