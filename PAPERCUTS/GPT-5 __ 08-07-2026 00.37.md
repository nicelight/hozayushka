# Papercuts

- Initial read-only JSON schema probe with the default `ajv` entrypoint failed because it did not load the task schema's Draft 2020-12 meta-schema; the local `ajv/dist/2020` entrypoint is available.
