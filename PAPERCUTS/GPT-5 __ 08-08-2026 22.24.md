# Workflow papercut

- Inline Node schema/link check was initially not launched because backticks in
  the shell command collided with the outer tool template literal. The check
  was rerun with the command quoting corrected.
