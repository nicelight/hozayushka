# Papercuts — GPT-5 — 2026-08-05

- A second `./gradlew clean assembleDebug testDebugUnitTest` was started while
  the first invocation was still writing `app/build`; the second run failed at
  `:app:clean` because the build directory was busy. Subsequent process/status
  inspection showed only idle Gradle daemons. The gate was rerun sequentially.
- An overly broad boundary scan included test-only fixture adapter imports and
  reported a false failure. The scan was narrowed to production non-owner roots
  plus a separate test check for provider-request construction.
- A static receipt command initially failed before execution because an
  over-escaped `find` grouping broke shell parsing. The command was corrected
  and rerun successfully; no project check or evidence state was changed by
  the failed parse.
- Parallel context reads exceeded the tool output cap and were rerun as
  targeted reads; no project state was changed.
- The first bulk canonical-boundary repair patch missed one changed draft line
  and was rejected before writing; the same repair was applied in smaller
  scoped patches.
- Verifier-owned package/static check could not invoke `aapt` by bare command
  because it is absent from PATH; the APK was already built and the check was
  rerouted to the Android SDK tool path without changing project state.
- The first re-verification evidence patch was rejected because its large
  context block did not match the current `verification.md`; no file was
  written by the rejected patch, and the update was split into smaller scoped
  patches.
- Starting the API 35 AVD for an ad-hoc target probe caused the emulator QEMU
  main loop to hang and ADB to disappear before the Foundation probe; the
  target process was not retained as an active task requirement, and the
  operator subsequently deferred all target checks until application readiness.
