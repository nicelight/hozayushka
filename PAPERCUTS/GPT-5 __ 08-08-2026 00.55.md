# Papercuts

- Shell inspection commands failed because a generated multi-file command was
  composed without separators, followed by a quoting error in a read-only
  validation command. Reissued the checks with explicit separators and simpler
  quoting; no project state was affected.
