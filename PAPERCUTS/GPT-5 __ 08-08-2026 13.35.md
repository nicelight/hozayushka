# Workflow papercut

- `avdmanager list avd` reports a missing system-image `devices.xml` before still returning the valid `Tecno_Pova_6_API_35` AVD; use the returned AVD record plus its `config.ini` for profile facts instead of treating the warning as a failed lookup.
- A shell default-value expression embedded in a JavaScript template literal was parsed as JavaScript and rejected before execution; avoid `${...}` shell expansions in orchestrated template strings or build the default with a shell `if`.
- The project compiles against API 35, but `/home/serg/Android/Sdk/build-tools/35.0.0/aapt` is not installed at that assumed path; resolve the available Build Tools version before invoking `aapt`.
- A tap estimated from the rendered screenshot preview missed Android's immersive-mode button because preview coordinates did not match the device bounds; query `uiautomator` bounds before coordinate input.
- A combined documentation patch used a line break that did not exactly match the dirty target file and was rejected atomically; anchor edits to the smallest unchanged surrounding lines when preserving existing user changes.
- `pgrep -f` with an unanchored AVD pattern also matched the inspecting shell command; anchor the full QEMU executable path or inspect `pgrep -af` output before reporting a process PID.
