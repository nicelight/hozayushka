## Papercut

- The first broad `apply_patch` for TASK-025 failed because the combined
  context did not match the card's current field ordering. The repair was
  safely retried as smaller field-local patches.
- A final inline validation probe had one parenthesis typo; it was corrected
  and rerun without touching project artifacts.
